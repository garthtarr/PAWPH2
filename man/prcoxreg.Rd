% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prcoxreg.R
\name{prcoxreg}
\alias{prcoxreg}
\title{Fit the PAWPH robust Cox regression model}
\usage{
prcoxreg(
  y,
  X,
  seed,
  alpha = 1,
  cond.CI = FALSE,
  pout.max = 0.2,
  permu.weight = rep(1, n),
  ...
)
}
\arguments{
\item{y}{Survival outcome matrix (n x 2): time and event indicator. Typically
created with \code{\link[survival:Surv]{survival::Surv()}}.}

\item{X}{Predictor matrix (n x p).}

\item{seed}{Integer seed for reproducible cross-validation fold assignment.}

\item{alpha}{Numeric vector of penalty mixing weights to sweep over via
\code{\link[=cv.prcox.adap]{cv.prcox.adap()}}. Values in (0, 1): proportion of penalty allocated to
per-observation intercepts. Use \code{alpha = 1} (default) to skip variable
selection and detect outliers only.}

\item{cond.CI}{Logical. If \code{TRUE}, compute bootstrap confidence intervals for
\code{betaHat_re} via \code{\link[=SE.boot]{SE.boot()}}. Default \code{FALSE}.}

\item{pout.max}{Maximum proportion of observations that may be flagged as
outliers. Default \code{0.2} (20\%). Set \code{NULL} to disable the constraint.}

\item{permu.weight}{Numeric vector of length n giving per-observation weights
applied to the event indicator in \code{\link[=ncvsurv]{ncvsurv()}}. Default is \code{rep(1, n)}.}

\item{...}{Additional arguments passed to \code{\link[=cv.prcox.adap]{cv.prcox.adap()}}.}
}
\value{
A list (the result of \code{\link[=cv.prcox.adap]{cv.prcox.adap()}}) with the following
additional or modified elements:
\describe{
\item{\code{betaHat}}{Coefficient estimates from Step 1 variable selection
(length p). Zero for unselected variables.}
\item{\code{betaHat_re}}{Refined coefficient estimates from Step 3 (length p).
These are the primary estimates for inference.}
\item{\code{gammaHat}}{Per-observation intercept estimates (length n). Values
far from zero indicate outliers; negative values correspond to
unexpectedly long survival (masking outliers), positive values to
unexpectedly short survival (swamping outliers).}
\item{\code{opt.alpha}}{Selected alpha value.}
\item{\code{cve}}{Cross-validation error path.}
\item{\code{CI}}{(If \code{cond.CI = TRUE}) Matrix (7 x p) of confidence interval
summaries with rows: SE, lower_n, upper_n, lower_q, upper_q, z.stat,
pvalue.}
\item{\code{se.res}}{(If \code{cond.CI = TRUE}) Full bootstrap result from
\code{\link[=SE.boot]{SE.boot()}}.}
}
}
\description{
The main user-facing function for the Penalized Adaptive Weighted
Proportional Hazard (PAWPH) model. Implements a three-step procedure for
simultaneous variable selection and outlier detection in survival data:
}
\details{
\strong{Step 1 — Variable selection} (when \code{alpha < 1}): An adaptive lasso
cross-validation (\code{\link[=cv.prcox.adap]{cv.prcox.adap()}}) is run with the supplied \code{alpha} vector
to identify relevant covariates.

\strong{Step 2 — Outlier detection}: The event indicator is temporarily set to 1
for all observations (treating censored observations as events) and an
adaptive lasso is run on the selected covariates (or all covariates if none
were selected in Step 1). The resulting per-observation intercepts
(gamma_i) flag outliers: a large negative gamma_i indicates an observation
whose survival time is unexpectedly long given its covariates.

\strong{Step 3 — Refitting}: A standard unpenalised Cox regression is fit on the
selected covariates with the estimated gamma_i intercepts held fixed. This
gives refined coefficient estimates \code{betaHat_re} unconfounded by outliers.

After Step 2, gamma_i values for censored observations that are not strongly
outlying (\code{gamma_i > -2}) are set to zero, and after Step 3 all small
intercepts (\verb{|gamma_i| < 0.5}) are zeroed.
}
\examples{
\dontrun{
library(survival)
library(MASS)

set.seed(42)
n <- 300; p <- 8
X <- matrix(rnorm(n * p), n, p)
colnames(X) <- paste0("X", 1:p)
# True coefficients: X1 and X2 are active, rest zero
beta_true <- c(1, -0.8, rep(0, p - 2))
lp <- X \%*\% beta_true
time <- rexp(n, rate = exp(lp))
status <- rbinom(n, 1, 0.8)
y <- cbind(time, status)

res <- prcoxreg(y, X, seed = 100, alpha = seq(0.1, 1, by = 0.1))
res$betaHat_re   # refined estimates
res$gammaHat     # outlier scores
}

}
\references{
Breheny P and Huang J (2011). Coordinate descent algorithms for nonconvex
penalized regression. \emph{Annals of Applied Statistics}, 5(1), 232–253.
\doi{10.1214/10-AOAS388}
}
\seealso{
\code{\link[=cv.prcox]{cv.prcox()}}, \code{\link[=cv.prcox.adap]{cv.prcox.adap()}}, \code{\link[=prcox]{prcox()}}, \code{\link[=SE.boot]{SE.boot()}}, \code{\link[=getCI]{getCI()}}
}
