% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncvsurv.R
\name{ncvsurv}
\alias{ncvsurv}
\title{Fit a penalised Cox regression model (internal)}
\usage{
ncvsurv(
  X,
  y,
  penalty = c("MCP", "SCAD", "lasso"),
  gamma = switch(penalty, SCAD = 3.7, 3),
  alpha = rep(1, p),
  lambda.min = ifelse(n > p, 0.001, 0.05),
  nlambda = 100,
  lambda,
  eps = 1e-04,
  max.iter = 10000,
  convex = TRUE,
  dfmax = p,
  penalty.factor = rep(1, ncol(X)),
  warn = TRUE,
  returnX,
  intercept = rep(0, n),
  permu.weight = rep(1, n),
  ...
)
}
\arguments{
\item{X}{Predictor matrix (n x p). Columns corresponding to augmented
identity matrix (per-observation intercepts) are added by \code{\link[=prcox]{prcox()}}.}

\item{y}{Survival outcome matrix with two columns: time and event indicator.}

\item{penalty}{Penalty type: \code{"MCP"}, \code{"SCAD"}, or \code{"lasso"}.}

\item{gamma}{Concavity parameter for MCP/SCAD.}

\item{alpha}{Per-variable penalty mixing parameters (vector of length p).}

\item{lambda.min}{Smallest lambda as a fraction of lambda.max.}

\item{nlambda}{Number of lambda values in the path.}

\item{lambda}{Optional user-supplied lambda sequence.}

\item{eps}{Convergence tolerance.}

\item{max.iter}{Maximum coordinate descent iterations.}

\item{convex}{Check for local convexity? Default \code{TRUE}.}

\item{dfmax}{Maximum number of active variables.}

\item{penalty.factor}{Per-variable penalty multipliers. Set to 0 to leave
a variable unpenalised.}

\item{warn}{Warn if algorithm does not converge?}

\item{returnX}{Return the standardised design matrix in the output?}

\item{intercept}{Numeric vector of length n giving per-observation intercepts
(gamma_i values). Defaults to \code{rep(0, n)}. This is the key modification
enabling PAWPH outlier detection.}

\item{permu.weight}{Numeric vector of length n giving per-observation weights
applied to the event indicator. Defaults to \code{rep(1, n)}.}

\item{...}{Additional arguments (ignored).}
}
\value{
An object of class \code{c("ncvsurv", "ncvreg")}.
}
\description{
Fits a regularisation path for Cox proportional hazards regression using
lasso, MCP, or SCAD penalties. This is an internal function adapted from
\code{ncvreg::ncvsurv()} with the addition of per-observation \code{intercept} and
\code{permu.weight} arguments required by the PAWPH method.
}
\references{
Breheny P and Huang J (2011). Coordinate descent algorithms for
nonconvex penalized regression. \emph{Annals of Applied Statistics}, 5(1),
232–253. \doi{10.1214/10-AOAS388}
}
\keyword{internal}
