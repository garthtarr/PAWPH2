% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{SE.boot}
\alias{SE.boot}
\title{Bootstrap standard errors for PAWPH coefficient estimates}
\usage{
SE.boot(
  orig_y,
  orig_X,
  B = 100,
  seed = 1234,
  ncores = max(1, parallel::detectCores() - 1),
  ...
)
}
\arguments{
\item{orig_y}{Survival outcome matrix (n x 2) from the original dataset.}

\item{orig_X}{Predictor matrix (n x p) from the original dataset.}

\item{B}{Number of bootstrap replicates. Default \code{100}.}

\item{seed}{Integer seed for reproducible bootstrap sampling. Default \code{1234}.}

\item{ncores}{Number of parallel workers. Default is
\code{max(1, parallel::detectCores() - 1)}. Set to \code{1} to run sequentially.}

\item{...}{Additional arguments passed to \code{\link[=prcoxreg]{prcoxreg()}} for each bootstrap
replicate (e.g., \code{alpha}, \code{pout.max}).}
}
\value{
A list with elements:
\describe{
\item{\code{betaHat}}{Matrix (B x p) of bootstrap \code{betaHat_re} estimates.}
\item{\code{res}}{List of length B containing the full \code{\link[=prcoxreg]{prcoxreg()}} result
for each bootstrap replicate.}
}
}
\description{
Computes bootstrap standard errors for the refined coefficient estimates
(\code{betaHat_re}) from \code{\link[=prcoxreg]{prcoxreg()}} by refitting the model on \code{B} bootstrap
samples in parallel.
}
\seealso{
\code{\link[=prcoxreg]{prcoxreg()}}, \code{\link[=getCI]{getCI()}}
}
