% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prcox.R
\name{prcox}
\alias{prcox}
\title{Fit a PAWPH base model}
\usage{
prcox(
  y,
  X,
  penalty.factor = c(rep(0, p), rep(1, n)),
  lambda.min = 0.05,
  lambda,
  ...
)
}
\arguments{
\item{y}{Survival outcome matrix with two columns: time and event indicator.
Typically created with \code{\link[survival:Surv]{survival::Surv()}}.}

\item{X}{Predictor matrix (n x p).}

\item{penalty.factor}{Numeric vector of length \code{p + n} giving per-variable
penalty multipliers. Defaults to \code{c(rep(0, p), rep(1, n))} — covariates
unpenalised, intercepts penalised.}

\item{lambda.min}{Smallest lambda value as a fraction of lambda.max.
Default is \code{0.05}.}

\item{lambda}{Optional user-supplied lambda sequence. If missing, an
automatic sequence is computed.}

\item{...}{Additional arguments passed to \code{\link[=ncvsurv]{ncvsurv()}}.}
}
\value{
An object of class \code{c("ncvsurv", "ncvreg")} as returned by
\code{\link[=ncvsurv]{ncvsurv()}}, fit on the augmented design matrix \verb{[X | I_n]}.
}
\description{
Augments the predictor matrix \code{X} with an n x n identity matrix (one column
per observation), then fits a penalised Cox regression path via \code{\link[=ncvsurv]{ncvsurv()}}.
The augmented columns allow the model to learn per-observation intercepts
(gamma_i), which are the basis of PAWPH outlier detection.
}
\details{
The covariate columns in \code{X} are left unpenalised by default
(\code{penalty.factor = 0}), while the augmented intercept columns are penalised
(\code{penalty.factor = 1}). This can be overridden via the \code{penalty.factor}
argument.
}
\examples{
\dontrun{
library(survival)
library(MASS)
set.seed(1)
n <- 100; p <- 5
X <- matrix(rnorm(n * p), n, p)
y <- cbind(time = rexp(n), status = rbinom(n, 1, 0.8))
fit <- prcox(y, X)
}

}
\seealso{
\code{\link[=prcoxreg]{prcoxreg()}}, \code{\link[=cv.prcox]{cv.prcox()}}
}
