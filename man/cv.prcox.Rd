% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv-prcox.R
\name{cv.prcox}
\alias{cv.prcox}
\title{Cross-validation for the PAWPH base model}
\usage{
cv.prcox(
  y,
  X,
  nfolds = 10,
  seed,
  penalty.factor = c(rep(0, p), rep(1, n)),
  pout.max = NULL,
  dfmax = 100,
  ...
)
}
\arguments{
\item{y}{Survival outcome matrix (n x 2): time and event indicator.}

\item{X}{Predictor matrix (n x p).}

\item{nfolds}{Number of CV folds. Default is \code{10}.}

\item{seed}{Optional integer seed for reproducible fold assignment.}

\item{penalty.factor}{Per-variable penalty multipliers (length \code{p + n}).
Defaults to \code{c(rep(0, p), rep(1, n))}.}

\item{pout.max}{Maximum allowable proportion of observations flagged as
outliers. Lambda values that exceed this proportion have their CV error set
to \code{Inf} and are effectively excluded from selection. Set to \code{NULL} to
disable (default \code{NULL}).}

\item{dfmax}{Maximum number of active variables in the model. Passed to
\code{\link[=prcox]{prcox()}}.}

\item{...}{Additional arguments passed to \code{\link[=prcox]{prcox()}}.}
}
\value{
A named list with elements:
\describe{
\item{\code{betaHat}}{Estimated coefficients for the \code{p} covariates at the
optimal lambda.}
\item{\code{gammaHat}}{Estimated per-observation intercepts (length n) at the
optimal lambda.}
\item{\code{fit}}{The full \code{\link[=prcox]{prcox()}} fit object (all lambda values).}
\item{\code{cve}}{Vector of cross-validation errors (mean clipped neg
log-likelihood) for each lambda.}
\item{\code{cve.min}}{Minimum CV error.}
\item{\code{opt.lam}}{Optimal lambda value.}
\item{\code{lik.val}}{Matrix of per-observation validation likelihoods.}
\item{\code{lambda}}{Lambda sequence used.}
}
}
\description{
Performs k-fold cross-validation for \code{\link[=prcox]{prcox()}} to select the regularisation
parameter lambda. Event status is stratified across folds so that each fold
has approximately the same proportion of events. The validation metric is the
mean negative log-likelihood, clipped at the 80th percentile to reduce the
influence of extreme values.
}
\seealso{
\code{\link[=prcox]{prcox()}}, \code{\link[=cv.prcox.adap]{cv.prcox.adap()}}, \code{\link[=prcoxreg]{prcoxreg()}}
}
