% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv-prcox.R
\name{cv.prcox.adap}
\alias{cv.prcox.adap}
\title{Adaptive lasso cross-validation for the PAWPH model}
\usage{
cv.prcox.adap(
  y,
  X,
  seed,
  alpha = 1,
  penalty.factor = rep(1, p + n),
  lambda.min = 0.05,
  pout.max = NULL,
  ...
)
}
\arguments{
\item{y}{Survival outcome matrix (n x 2): time and event indicator.}

\item{X}{Predictor matrix (n x p).}

\item{seed}{Optional integer seed for reproducible fold assignment.}

\item{alpha}{Numeric vector of penalty mixing weights (between 0 and 1) to
sweep over. Default is \code{1} (intercepts only penalised). Values close to 0
favour covariate selection; values close to 1 favour intercept selection.}

\item{penalty.factor}{Optional additional per-variable multipliers (length
\code{p + n}). Defaults to all-ones.}

\item{lambda.min}{Smallest lambda as a fraction of lambda.max. Default \code{0.05}.}

\item{pout.max}{Maximum proportion of observations that may be flagged as
outliers. Passed to \code{\link[=cv.prcox]{cv.prcox()}}.}

\item{...}{Additional arguments passed to \code{\link[=cv.prcox]{cv.prcox()}}.}
}
\value{
The \code{\link[=cv.prcox]{cv.prcox()}} result list from the best \code{alpha}, with an
additional element \code{opt.alpha} giving the selected \code{alpha} value.
}
\description{
Implements a two-step adaptive lasso procedure for the PAWPH model over a
grid of \code{alpha} values (mixing weight between covariate and intercept
penalties). For each \code{alpha}:
}
\details{
\enumerate{
\item An initial \code{\link[=cv.prcox]{cv.prcox()}} fit is computed.
\item Adaptive lasso weights are derived from the initial estimates:
\verb{1/|beta_j|} for covariates, \verb{1/|gamma_i|} for intercepts.
\item A second \code{\link[=cv.prcox]{cv.prcox()}} is fit with these weights.
}

The \code{alpha} value minimising the second-step CV error is selected.
}
\seealso{
\code{\link[=cv.prcox]{cv.prcox()}}, \code{\link[=prcoxreg]{prcoxreg()}}
}
